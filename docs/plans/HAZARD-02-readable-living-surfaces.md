# HAZARD-02 — readable living surfaces

Execution authorized by the Human's wall→hazard priority. Root Astra writes on
`codex/hazard-surface-polish`; actual Sol independently reviews. Baseline is exact
published web0.22.13 at e59d0f9, runtime dfe04a93. This is the next bounded
Plan02/04 surface pass, not whole-plan, connected-pit or native acceptance.

Approved water/lava/poison sheets currently repeat over4.6/4.6/4.2tiles. The
whole connected shape is eroded/blurred before both texture and marks are painted.
Poison marks use5→-15px translation in an SVG tile coordinate system, a scale
mistake that can move a small bubble many tile units. Whole-water/lava opacity
cycles also change the full receiver instead of creating localized life.

Implement catalog-owned smaller texture repeats, crisp exact connected clipping,
gentle local water crests, contained molten currents/hotspots and bounded rising
poison bubbles. Keep the base opaque and steady, with distinct static patterns
and no raised lip, bevel, shadow or changed hazard rules. Bubble displacement
uses its own bounding box, not pixel distances interpreted as tile units.
Reuse existing CSS/SVG owners, immutable world geometry and approved pixels.
No renderer/dependency/new media, whole-scene filter, per-tile timer or JS loop.
Lite keeps cheaper base art without live marks; Reduced/Static retain stable
readable marks and the same semantic information. Full effects stay inside
the original hazard union at every phase and during camera travel.

Qualify actual water/lava/poison views and paired old/new moving routes at two
DSF2 viewports. Check no terrain mutation, exact engine steps, rounded/diagonal
boundaries, wall/pit exclusion, equipped actors and current jump/mode handoffs.
Measure idle/moving frame/trace work, bytes and surface/resource bounds; no
physical iPad claim. Root provisional budget: +1KiB gzip9JS/+300gzip9CSS,
zero public/decoded-image growth; actual allocation depends on measured result.
Retain failures and public identity checks. Publish once through existing Git,
then docs-only closure. P13 wall beauty stays open; Q04 opens only for a new build.

After this slice, latest physical-loot feedback advances ahead of the remaining
celebration/learning work in dependency order. No new economy or XP is claimed
by this hazard implementation.
