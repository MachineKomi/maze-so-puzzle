# DELIGHT27 — an earned keepsake moment

Authorized DELIGHT-02B C after published26. Astra is sole runtime writer on
codex/earned-keepsakes; actual Sol independently reviews. Live remains26 until
qualification. This closes C, then LEARN-01 including STORY-RESCUE-01 is next.

After Next successfully writes existing progress, derive newly added known
sticker/medal/badge IDs from actual before/after profiles. Load the next maze,
then admit one ephemeral event. No earlier banking, storage polling, new receipt
ledger or future collection award. All earned IDs remain in the Book.

One approved keepsake is featured in stable sticker/medal/badge order, with
"Saved in your Book" and an additional-keepsakes count. The card appears only
after chapter/critical dialogs close and the next maze is playable. Generated
mazes follow the same rule without a chapter dialog. It never becomes a modal,
changes the camera viewport or catches pointer input. A pure corner calculation
keeps its36%×40% footprint clear of Ame's surrounding3×3 tiles. Centered starts
use a60%×28% horizontal card so artwork and text remain legible; smaller bounded
fallback footprints retain the same exclusion rule. Current authored
and generated starts must prove a valid corner; do not silently assume arbitrary
future starts fit. No location reads in movement or decorative per-frame JS.

The art flourishes for900ms Full/450ms Lite, then rests; Reduced/Static remain
still. One4.5-second timer bounds stationary reading. Movement, navigation,
hidden page, portrait, resize or a new critical overlay after activation cancels
the event. Calm preferences latch its animation quiet. Waiting behind a chapter
is silent; backgrounding drops waiting events too. Reload/retry cannot replay.
One existing reward/stamp cue uses the current SFX bus/mute only at activation.
Remove the old precommit rewardSoundTimer, including its after-Stay leak.

## Boundaries and acceptance

Use the [verified commit/context table](DELIGHT-26-calm-pickup-presence.md#c-handoff--verified-against-unchanged-app-owner-at-fd1f9e6).
Stay/tester/restart, denied journal/profile writes and unsupported future profile
continuation emit nothing. A successful profile write followed by run-clear
failure still earns once. Recovered receipts and currency-only changes emit none.

Bound: one event, one mounted card, one existing art image with128px rendition
request, at most seven extra DOM elements, one finite transform animation and
one timeout; no media pack, canvas, RAF, dependency or gameplay-state schema.
Check actual budgets against26 before promotion; no silent allocation increase.
Capture real earned moments in phone expanded/folded/Classic and desktop, all
comfort modes and larger text, together with actual input, save failures,
coalescing, disappearance and no replay. Verify actual corner bounds against
the player's protected tiles and controls. Compare exact source-matched timing
and retained resources, keep unfavorable tails and independent Sol disposition.

One existing dist and small frozen26 entry bundle sharing public media; all new
keepsake27-* proofs under C:/GameDev/maze-game-qa/performance. Record counts/bytes
in the artifact ledger. No deletion/archive or extra repository/native copy.
Exact reviewed-head CI precedes deployment; both public origins follow, then
prove the docs-only Vercel skip. No physical Apple/3GB/native acceptance inferred.

Q13/P27 will ask whether the saved keepsake is understandable and satisfying,
and whether it stays clear of Ame and the controls. Existing Q12/Q11 and Q08
device/cold-Power requests remain open without blocking independent work.
