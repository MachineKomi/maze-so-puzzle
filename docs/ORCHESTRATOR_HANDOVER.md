# Maze so Puzzle — orchestrator entry point

Begin with [JOINT_ORCHESTRATION_STATE.md](JOINT_ORCHESTRATION_STATE.md).
It is the single current-state authority for checkpoints, releases, active work,
Human decisions, risks, programme order, evidence, recovery and Sol–Astra protocol.
Do not append new status layers to this entry point.

Read the complete joint state, inspect live Git/ownership, then read the linked
vision, roadmap, execution prompts and relevant full plan/addenda/backlog slices.
Historical assignment text never overrides subsequent accepted checkpoints or
the Human's latest decisions. Preserve completed art approvals and user work.

The Human is manually moving orchestration into **Sol Astra MsP Collab** and
switching between Sol and Astra in that shared task. The repository carries the
context. The next model is **GPT-5.6 Sol — HIGH**. Only one runtime writer works
at a time; the other model reviews read-only.

The immediate joint task is reviewing the final Opus 5 pack. Sol performs the
first product/intent/evidence assessment; Astra challenges it technically; Sol
reconciles the positions for Human approval. No proposal is implemented simply
because it appears in the review. Use the [joint ledger](reviews/2026-09-05-sol-astra-opus5-disposition.md).
Agent 04 and further runtime work remain held under the canonical decision gates.

Previous handover history is preserved at
[the pre-transfer Git checkpoint](https://github.com/MachineKomi/maze-so-puzzle/blob/f1166b0e7dd5d25b50e74abe1f539e3e7cd18d63/docs/ORCHESTRATOR_HANDOVER.md),
in dated playtests/reviews and in Git history. Old Agent 01-running, unaccepted
candidate, release-pending and parallel-runtime paragraphs there are history,
not current instructions. Do not recreate those competing status layers.
