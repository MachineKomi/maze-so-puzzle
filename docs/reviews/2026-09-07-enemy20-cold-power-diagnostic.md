# Enemy20 — retained cold Power appearance limitation

Runtime6f083f4; helper/test checkpoint ebb2b17. This is a bounded diagnostic,
not Apple/native acceptance or a new timing qualification cohort. It arose
while qualifying enemy Gold/Science, before publication.

The earlier53-case matrix passed before the visible-interval correction.
The corrected source then passed54/55 cases: Lite's last snapshot still had a
valid physical claim at4200ms, with19 arrivals minus7 currency credits proving
all12 Power arrivals. The fixed wall-clock idle assertion was wrong because a
newly admitted Lite slot owes its own readable interval. The replacement test
waits for idle, then proves a stable currency/drop signature for1600ms within
a bounded deadline. It does not accelerate collection or relax conservation.

An immediate-entry rerun separately exposed real cosmetic potion cancellation.
Full had peak2 and no sampled visible tokens in one run; Lite showed2 before a
158.8ms frame gap. Instrumented reruns had safe target coordinates, no post-emit
resize, and2 tokens before156–203ms gaps. The first-paint correction works;
the existing later-stall policy expires cosmetic tokens instead of simulating
a large catch-up burst/audio. Immediate engine Power remains correct. This
failure must not be renamed a successful cold-appearance check.

## Same-sequence discriminator against live19

`enemy20-cold-power-fresh-edge/report.json` serves exact frozen19 and20 entries,
with unchanged shared media. Eight fresh Edge processes cover both builds twice
at780x312 and1194x834, DPR2, CPU1, zero extra post-entry settle time, real potion
input and reversible return. Both builds lose cosmetic arrivals in **4/4 rows**;
all retain Power4, correct position/steps, no terrain mutations, broken images
or page errors. Labels include each layout's warmup pair and are not pooled
with five-pair qualification. There is no claim that a1-pair timing comparison
establishes a regression, improvement or complete cause.

The measured-pair maximum rAF intervals are183.7→183.6ms phone and217→233.7ms
large layout. The companion same-browser pilot loses only its first candidate
warmup's cue; all subsequent contexts deliver2. A fresh process for each side
is therefore essential to this diagnostic. Browser rAF timestamps are not the
same clock samples as the token integrator's `performance.now()` deadlines.

Report SHA-256:0256d4812260c197deb187c290d7d0c74024918ea6640fb9dc98e71e90bd6c3e.
Same-browser pilot SHA-256:55ae3312264e8ee844fe9abf2423059e1388a609e0d06ad3b0c04d901750d144.
The first Chromium pilot's camera-transform assumption failed on this clamped
route; its one row is retained, not qualification. The corrected route checks
real movement/return and permits a clamped camera.

## Release boundary and next investigation

Actual Sol independently reviewed the source, frames/targets and both-version
discriminator. This inherited cold limitation does not block B after loaded
Power and new-currency qualification. `enemy20-loaded-power-browser` passes5/5
with an explicit500ms post-entry stabilization, exact2 potion/12 combat Power
arrivals, stable rest, capacity and conserved currency. This is loaded evidence,
not cold success. Its report SHA-256 is
b1ad5e01b5bb2988f20947d0422924a452af1af8540359cabdcbb64302b800cd.

Keep PERF-COLD-POWER open alongside Q08: compare fresh-process startup/first
Power with actual affected-device observations, separating entry/layout/render
work from the cosmetic later-stall policy before another runtime change. Do not
extend stall catch-up or audio blindly. Any later resume/minimum-visible-time
design must retain explicit combat deadlines, bounded work, cancellation and
immediate solvability-critical Power. This finding does not establish a RAM
floor or explain the original Apple camera complaint. Source-matched enemy and
ordinary-camera five-pair cohorts remain the separate B release gates.

All packets live under external QA `performance/`, inventoried in the
[artifact ledger](../LOCAL_ARTIFACT_LEDGER.md). No deletion/archive is authorized.
