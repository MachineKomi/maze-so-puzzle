# DELIGHT27 C — earned keepsake qualification

Frozen runtime **fdc60f279c2c08e0aed8992ecc6dea55a0b391c7**, candidate web0.22.27.
Compare published26 runtimefd1f9e6 using four frozen entry files with shared
unchanged media. [Contract](../plans/DELIGHT-27-earned-keepsakes.md) and
[actual independent Sol review](2026-09-07-keepsake27-sol-review.md) govern this
bounded C checkpoint. Local qualification is complete and actual Sol supports
bounded web promotion. Exact reviewed-head CI precedes publication; the public
receipt will record deployment and public checks separately.

## Behavior and resource bounds

Actual newly saved sticker/medal/badge IDs are derived only after Next's existing
successful profile write, after loading the next level. One featured approved
artwork plus an additional-awards count waits silently behind chapter instructions.
The nonmodal, pointer-transparent card clears Ame's surrounding3×3 tiles, keeps
the current maze/HUD geometry, and disappears on first movement or interruption.
Generated destinations need no special modal. The Book retains every earned ID.

Full art motion ends900ms; Lite450ms; Reduced/Static are still. One4.5-second
deadline bounds stationary reading and survives StrictMode effect replay without
replaying sound. Hidden, portrait, resize, navigation or active overlay cancels.
One existing reward/stamp cue honors mute/SFX gain. The old projected reward timer
and after-Stay cue leak are removed. No award arithmetic, ledger, schema, media,
dependency, canvas or RAF is added. At most six extra DOM nodes and one art image
are mounted; no new budget allocation. The approved art already appears in the
projected victory and Book; this card requests a128px rendition.

JS180835/181169 gzip bytes, CSS26848/31622 and media155555324/165031011 all pass.
Relative to26: +752 compressed JS bytes, +266 CSS bytes, zero media bytes.
Full project check passes **775 tests in79 files**, plus production build.
Frozen runtime [CI34149568397 / Browser build239](https://github.com/MachineKomi/maze-so-puzzle/actions/runs/34149568397)
passes. Final reviewed-head CI remains required; Windows compilation is not native
release acceptance. Runtime-input SHA256:
`e544c2bf5e6f4ab59283364cb83d1129e56af43920e40ec9f9a1a6bff31b5537`.
Dist fingerprint: `368b24db438a3dd28f7199dbc83262a80926da93d710521b5251d6aaaebcd691`.

## Correctness, visuals and retained failures

All raw packets live under C:/GameDev/maze-game-qa/performance.

- `keepsake27-final-browser`: **33/33** on frozen runtime, comprising23 C contracts
  and10 existing XP/save contracts. Real routes cover expanded/folded/Classic,
  780/844 phone,1280 desktop, all comfort modes, Stay/tester, denied journal/profile
  writes, successful-write/failed-clear, future-profile temporary adventure, hidden
  waiting/active cancellation, active More, first move, timeout, resize/enlarged
  text, generated destination and actual dev StrictMode expiry.
- `keepsake27-extra-browser`: four passing cases include centered text bounds,
  exactly one scheduled stamp at SFX.85 only after Start (zero precommit), no
  scheduled stamp at SFX0, and real native CSS settling before receipt expiry.
  Two additional fixture/assertion failures and two not-run cases are retained.
- `keepsake27-extra-r2`: **4/4**, completing actual fifth-maze medal/badge and
  badge-featured cases, recovered completion receipt/no replay and the refined
  enlarged-text check. Actual earned-ID arrays match the before/after profile.
- The latest passing union is **39 distinct affected contracts**, not one clean39
  run. Extra packet page-error receipts are empty. Sound scheduling is proven;
  this is not a Human listening assessment.
- `keepsake27-public26-baseline`: **2/2** matched phone/desktop actual routes and
  screenshots of the prior view. Live26 HTML/JS/CSS hashes were independently
  matched to frozen26 in `keepsake27-summary/public26-identity.json` before promotion.

Sol identified the original null-placement case for centered Classic starts and
the StrictMode timer cleanup defect. Both were corrected. The initial narrow
vertical fallback then visibly clipped copy at780 Classic; the final60%×28%
horizontal fallback retains the same protected region and gives60.10px art with
complete copy in a180.96×84.45px card. Exhaustive pure tests cover all16 current
Classic starts and every whole-tile position in a6×6 camera. Current generated
sizes start at9×9; arbitrary future sub-six geometry is not accepted by this proof.

Retain r1/r2 early harness failures: first completion also earns Twinkle Toes,
and the next maze's first safe move must be derived rather than assumed ArrowUp.
R3 retains actual narrow-card clipping and a future-profile fixture that wrongly
expected Continue; the protected profile correctly offers Begin. The corrected
test plays the real temporary first maze. The extra milestone fixture initially
omitted legitimately earned Mighty Adventurer. The first inner-text assertion
mistook bold glyph ink over a32px line box (scroll34px) for clipping; the detailed
rerun records the actual containing DIV209×86/scroll209×87, with all text inside
the card. The real earlier narrow-card overflow remains a defect, not relabeled.

## Source-matched paired performance

One Edge workload at a time, DPR2, CPU4,844×390 and1280×720. Five alternating
pairs/profile plus excluded warmups in each cohort: **72 rows,60 measured**.
Untraced ordinary movement uses the same folded pickup-dense16-step reversible
route. Earned timing and separate traced work use the same actual Next commit,
then chapter dismissal, finite card and five seconds stationary. This is not cold
start. The pilot's raw failure is retained: chapter Start legitimately changes
discovered species. Final comparison excludes only those two discovery arrays
while requiring every completion/reward field to remain fixed after the write.

All measured rows have zero page errors, broken images or terrain mutations;
movement returns to its initial position, and earned scenes remain at0 steps.
Every candidate earned row observes exactly one card, every baseline zero,
and no card remains at the end. Six nodes/one image are added only while active:
phone271→277→271; desktop259→265→259. Images30→31→30 and27→28→27 respectively.
Final traced layers remain27 phone/20 desktop on both sides; largest reported
CSS layer area remains1,480,160. This does not measure allocated GPU memory.

Median per-run p95 is16.8ms in every profile/cohort. Untraced movement phone has
baseline3 versus candidate5 intervals over20ms, worst33.4→33.5ms; retain this small
unfavorable count shift. Desktop has3→1, worst33.368→32.434ms. No moving interval
exceeds34ms. Untraced earned phone has1→0 intervals over20ms; desktop2→1; none
exceeds34ms. Traced earned phone has six over20 intervals and one50.1ms interval
on each side; desktop has five over20 on each side, worst33.5ms. No sustained
candidate-specific frame regression is evident, and universal tail parity is
not claimed.

Median traced totals in milliseconds (overlapping, not additive/GPU time):

| Profile | Paint26→27 | Raster26→27 | Layout26→27 | Style26→27 |
| --- | --- | --- | --- | --- |
| Phone |367.568→368.073|12.563→13.700|0.203→5.824|315.541→318.043|
| Desktop |373.478→377.882|11.007→11.615|0.204→6.006|210.487→212.185|

The finite card has a small measured rendering cost; it is not presented as a
performance improvement. Physical iPhone13/iPad8, Apple/WebKit,3GB, cold first-Power,
thermal and native acceptance remain open. Latest fully published Windows0.22.9;
native0.22.10 qualification remains unfinished.

| Packet | Report SHA256 |
| --- | --- |
| keepsake27-moving-frames |e9b4e475fcf9c8fb0d8e54938dd34e94cbab6f0916198ea9bd45a6e1e638712d|
| keepsake27-earned-frames |cb5d1d13487ff8fc8828faf562487541059962b9b8eeaa51e2c35d9f742e8936|
| keepsake27-earned-work |8f5b0edf01ef6fa8fd4248c84b3457f2637ea37824df24806177d0228d7564a8|

## Next

Q13/P27 asks whether the saved keepsake is clear, satisfying and out of the way.
Q12 pickups, Q11 layouts and Q08 affected Apple/cold-Power remain open. No immediate
Human decision blocks continued development. Once C is published, advance LEARN-01:
explicit Power shortfall, picture-led request-only hints and truthful rescue
outros, preserving existing reward/input/solver owners and wider roadmap.
