# LEARN-28 independent Sol review

**Reviewer:** Sol (GPT-5.6), independent read-only runtime review  
**Candidate:** `aa6a0a1d9617eab8f4b8e8bb6f022e6b87f83976` on `codex/readable-reasoning`  
**Status:** independent bounded-web promotion support

## Source findings

The candidate adds presentation around existing engine and search authority. The Too Strong result still comes from the unchanged `enemy-too-strong` event and therefore follows the engine's weapon-first check. The modal shows `enemyPower - playerPower`, keeps the true equality rule, and labels each existing search result with the Power that its engine event would add. Pending, found and empty results remain explicitly provisional: a card is a reachable opportunity rather than an immediate step or a promise that one gain closes the deficit. The bounded 2,048-state opportunity search, its cancellation and stale-result identity checks are unchanged.

The hint path remains opt-in, four-tiered and derived from the existing engine-replayed zero-rescue required route. My first review found new colour-specific key pictures, exact unseen guardian values and portal identity in early hints. The frozen source corrects those leaks. Tool pictures are generic approved assets; key and door use the neutral help image because the existing `ASSETS.key` is visibly the Blue Star key rather than a neutral key. Guardian labels and principles no longer disclose an unseen name or Power. The first two tiers still omit direction and target ID, and no new hint metadata is connected to the minimap or discovery owner. `HintThought` separates the first sentence for visual emphasis while preserving punctuation and omitting an empty support paragraph.

The Too Strong search result is now a polite status region. The equation, shortfall, opportunity gains and required-path action remain ordinary text and controls; the implementation adds no movement replay, automatic hint escalation or new input lock. Existing engine tests cover Power 1, 124, 125 and 150 against a Power 125 guardian with and without a weapon, proving weapon precedence, lower blocking, equality success and higher success. The two mounted 125-versus-250 images are explicitly typography-only stress cases; they demonstrate three-digit containment and are not evidence of an authored encounter or engine route.

Four story outros no longer assert that optional friends were rescued, and the finale no longer asserts that the ending Power is exactly 99. The additional sentence uses the current game state's rescued IDs and the current level's friend count. It therefore distinguishes zero, one, partial and complete attempts without consulting an earlier best or a projected reward. The complete case uses warm company copy because the existing perfect-rescue banner already supplies the exact all-friends result. This changes no completion, Stay/Next, reward or save owner.

## Visual and browser review

I freshly inspected the source-matched images in `C:/GameDev/maze-game-qa/performance/learn28-final-browser/learning`. The Power 6 versus 9 compact and enlarged phone states make the inequality and exact three-point deficit substantially easier to read than the public-27 baseline. The one visible Power-5 opportunity is presented as a possible gain, and the supporting sentence asks the player to keep comparing rather than claiming that the card is the complete route. The 125-versus-250 typography stress remains contained at 780 and 1280 widths.

At 200% root text on the short phone, the fixed footer necessarily leaves part of the result below the initial body view. The separate scrolled capture shows the complete opportunity gain and helper text inside the scrollable dialog body while both actions remain reachable. This is valid bounded reflow evidence, not a claim that every line is initially visible at that stress setting.

The Spring Boots tier-two picture and rule form one clear visual thought. Portal and guardian rules use generic help imagery without exposing a hidden variant. The earlier key screenshot with a Blue Star-looking generic asset is superseded by the frozen neutral-help mapping. The main 25-case final packet contains seven distinct picture categories. Because multiple mechanics now correctly share `navHelp`, that packet does not by itself cover every semantic mechanic.

I therefore freshly inspected all eight matching-key and matching-door images in the separate source-matched `learn28-followup-browser` packet. The neutral picture remains appropriate across all four tiers. The text keeps key and door objectives distinct, teaches the reusable matching relationship, and withholds direction until tiers three and four. That packet also covers the existing potion and combat reward receipts and cleanup in Full, Lite, Reduced and Static modes. It passed six of six cases. The evidence is 25 clean final cases plus six clean follow-up cases, or 31 distinct passing contracts; it is not one clean 31-case run.

The Moonlit zero, partial and complete captures agree with the actual attempt. Zero rescue invites a later return, one rescue reports one friend, and complete rescue avoids repeating the existing perfect banner. The revised chapter copy no longer contradicts the visible victory parade or saved reward breakdown.

The final source check log records 780 tests in 79 files followed by a successful TypeScript/Vite build. The browser evidence records 25 of 25 final cases and six of six follow-up cases. JS gzip9 is 181,303 bytes, 468 bytes above web27; CSS gzip9 is 26,913 bytes, 65 bytes above web27; no public media or dependency was added. The 134-byte JS amount beyond the prior 334-byte headroom is assigned to the registered UI allocation owner. I did not run these checks and treat them as Astra-owned evidence.

## Performance evidence

I independently checked the three completed report files and their aggregate. Each has 24 rows: four warmups and 20 measured rows, comprising five alternating pairs per 844 and 1280 profile under CPU4. The reports bind the frozen candidate and served bytes, and all 60 measured rows record no gameplay error, broken image or terrain mutation. Their SHA-256 values are:

- moving frames: `b7c7215e93c33e482dd0f30041433ad04916c6eecdee5c336dc706cc58897b7b`
- guidance frames: `9ee47dcdd0e3049ba3a25238940158d3eb61e9977db884be875c5b07d8529d2e`
- guidance work: `b97e066e67a46259320fd3f099b5fa8434025ef20d0f9157ca504ab13d2dd13d`
- aggregate: `46071455443a924e55e6ca34221400e410bec6448a29cfe0f5cbc5dc0d21b70f`
- entry identity: `d56a56948db74274c561ad9d625cb3fe98c85f2aa6f2779dc8026fbc1f6c295b`

Ordinary movement remains mixed and bounded. Every measured p95 is about 16.8 ms, no interval exceeds 34 ms, and the total intervals over 20 ms change from 12 to 9 at 844 and from 4 to 6 at 1280. The untraced guidance route also keeps every p95 about 16.8 ms. Its intervals over 20 ms are unchanged at 15 on the phone profile and 19 on desktop; intervals over 34 ms change from 8 to 7 and 6 to 7 respectively, with an essentially unchanged approximately 117 ms worst interval.

The guidance response is measurably slower. The median input-to-second-rAF proxy rises from 69.8 to 73.1 ms at 844 and from 68.7 to 74.0 ms at 1280, increases of about 3.3 and 5.3 ms. This proxy includes the bounded search and modal presentation; it does not prove strict input parity or a faster help response. In traced guidance work, median Layout rises by 8.072 ms at 844 and 5.160 ms at 1280. The candidate's traced worst interval rises from 117.0 to 133.6 ms and from 116.9 to 133.5 ms, while intervals over 34 ms rise from 13 to 15 in both profiles. Paint is slightly lower in both profiles, Raster changes by only about +1.183/-0.150 ms, and UpdateLayoutTree changes by +16.558/-17.413 ms over the complete route. The mounted modal adds two DOM nodes, one image already expected for the picture card, and no traced layer; ordinary movement resources are unchanged.

These data support a bounded web release. The exact arithmetic, progressive picture-led help and truthful rescue copy provide a material learning improvement without changing engine, search, save or movement authority. The finite guidance latency and traced tail increases are real and should remain in the qualification record, but I do not find them large enough to block this opt-in modal change given stable untraced p95s, unchanged guidance over-20 counts, no sustained resource growth and no ordinary-movement tail beyond 34 ms.

This disposition covers the frozen Chromium/Windows web evidence only. It does not establish family comprehension, screen-reader usability on a physical device, physical Apple performance, native acceptance, Human approval of the teaching result or educational efficacy.
