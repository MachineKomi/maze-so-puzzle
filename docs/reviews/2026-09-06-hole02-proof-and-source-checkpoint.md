# HOLE-02 proof repair and selected-source checkpoint

Source candidate `7df8b0f6ecb62cc4a6c29faa574f8afa607c2cbe`; final source/metadata
`96b898d5fe3497d53dcbfaaafd00a909ba3b1684`. Root Astra wrote; actual Sol performed
independent read-only source/receipt review and accepted this bounded preparation
checkpoint, conditional on the final metadata check. That check passed below.

## Delivered, not merely planned

- Current art validation no longer depends on the accidental presence of an
  optional ignored historical canary. Explicit artifact-self-consistency and
  current-input compatibility modes remain; mandatory approved v14 stays enforced.
- A reproducible committed ZIP contains the original approved index and48 children.
  [Offline recovery](../source-assets/evidence/README.md) verifies the original
  approval chain, restores missing files, refuses differing data and excludes
  unrelated extra files from its claim. Hashes remain raw bytes.
- Manifest drift names exact fields. The original stale state comprised196
  record JSON hashes plus the rights review and batch24 run record stored
  locally with CRLF despite Git/manifest LF. All198 restored bytes exactly matched
  existing Git blobs and recorded hashes; no semantic record/approval was edited.
  New pipeline/source inputs were then deliberately added to the derived manifest.
- Two immutable blank-canvas originals, exact prompts and native RGBA provenance
  are committed. Human selected rounded-square B and delegated implementation;
  [selection](../source-assets/calibrations/hole02/human-selection.json).
  Source growth is2,142,114 bytes, not runtime bytes. No source art is archived
  out of the project or deleted.
- A regeneratable compact [gallery](../../artifacts/art-proofs/hole02/index.html)
  compares current/B/A and six connected construction classes.16 cardinal masks
  and diagonal separation reuse the existing terrain union. Connected shapes are
  explicitly code-native previews, not published cutouts from the generated art.
- The [joint Claude review](2026-09-06-v0228-next-focus-disposition.md) and new
  iPad comparison are routed. Wall-only04-A no longer waits for unrelated
  ditch/PT36 gates; reward-first02 owns the satisfying presentation tranche.

## Qualification

| Evidence | Actual result |
| --- | --- |
| Clean detached checkout at7df8b0f, before restore | Current art gate failed23 missing mandatory-evidence diagnostics; no false pass. |
| Archive non-writing check |49 bound files verified;49 missing targets. |
| Explicit restore |49 files restored; runtime writes0. |
| Clean-checkout current art gate afterward |0errors /429 classified historical warnings. |
| Final checkout96b898d after formatting-only metadata follow-up |0errors /429warnings again; no local text/EOL workaround required. |
| Art unit suite |142/142 passed. Initial141-test attempt failed only old expected batch-owner count279; adding the two new originals legitimately changes it to281. Rejected attempt not counted as a pass. |
| Focused proof tests |6/6: explicit CLI selection, stale-source failure, unchanged raw hashes, manifest diagnostics, safe restoration/tampering, interrupted pack-pair retry. |
| Existing terrain-geometry suite |21/21 passed; code-native preview covers all16 masks and two diagonal loops. |
| Legacy optional artifacts mode |Passed checksum self-consistency only; no external authenticity claim. |
| Legacy current-inputs mode |Failed as expected on changed pipeline files and historical CRLF test; not waived or labelled compatible. |
| Browser gallery |1100x850 and568x500: all3images loaded,6shape SVGs, no horizontal overflow. Initial favicon404 corrected with an explicit empty favicon; no sprite failed. |
| Clean-checkout gallery reconstruction |Succeeded; registered review WebPs total82,742bytes, originals unchanged. |
| Runtime comparison to5b97434 |No `src/`, `public/`, Tauri, package or lockfile changes. No new game build/version, bundle allocation or runtime-performance claim. |
| Git diff whitespace |Passed after trimming one extra wrapper-only EOF line in new PROMPTS.md; exact submitted prompt text and generator originals unchanged. |

Raw local qualification evidence is in
`C:/GameDev/maze-game-qa/holes/hole02-preflight`. Final JSON
`final-96b898d.json` SHA-256
`245f7c5967ec94bd663a31e7c1129e7e045db8636ab93a5e3e8caab5df58b552`.
The clean checkout was `C:/GameDev/maze-game-qa/worktrees/hole02-proof-qual`;
this demonstrates a portable checkout with installed Python dependencies,
not a tested new-OS installer or fresh dependency installation.

## Limits and next delivery

No pit sprite/catalogue pointer or connected renderer has changed in the playable
game; v0.22.8 remains current. HOLE-02 still needs actual integration, seams,
cross-floor/quality checks and gameplay/native review. The new wall-only04-A
tranche can now proceed without waiting for its unrelated surfaces. PT36 stays
open for actor/held grounding. Physical iPad movement remains imperfect per
Human reports, including the build-unknown Chrome/Safari comparison.

Preserve142 art-test and exact-source qualification distinctions; do not call
the historical v0.22.8 release newly device-tested. Sol's archive-source acceptance
does not approve a future renderer or replace family playtesting. Restore retry
refuses a partial/different local member after an interrupted write; it never
silently overwrites it. Pack-pair publication has byte-identical retry recovery.
