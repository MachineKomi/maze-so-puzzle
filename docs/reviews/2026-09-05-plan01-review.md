# Plan 01 — root review of the finished candidate

Date: 2026-09-05. Baseline HEAD: `47bfff4ef6d2c973e7aae6d5d2ddbd646e9f2017`.
Decision: **return bounded corrections to the same Agent 01 task**.
No UI acceptance, movement implementation or FP-UI1 publication is claimed.

The Human authorized review → accepted UI checkpoint → MOVE-01 → FP-UI1.
The roadmap explicitly keeps incomplete UI prerequisites as return gates.
Root reviewed the candidate report, source/diff, full Plan-01 criteria and
saved layout/material evidence, with independent code and evidence reviewers.

## What is worth preserving

The extraction substantially reduces the historical CSS cascade and gives the
game a consistent board/deck composition, wrapping semantic inventory, useful
minimap, common dialogs, typed UI blocking, Sound port, motion preferences,
earned detail and short story host. Approved art and gameplay source identities
are preserved. The reports distinguish optical fallbacks, requested budget,
dirty timing, compilation and actual release evidence honestly.

The final shared browser report contains 18 passes, zero failures/flakes.
Its JS/CSS/font hashes match the reviewed working build. These are inspected
Agent-01 results, not a new root browser run. Root viewed the layout and styled
state sheets; their compact-phone cutoff and small blocker fallback confirm
the declared mandatory gaps. Six timing cohorts remain report-only and do not
prove smooth travel, device performance or comfort.

## Required corrections

| Finding | Evidence / impact | Acceptance needed |
| --- | --- | --- |
| Phone essentials scroll away | `AdventureHud.tsx` 36/41 and `hud.css`; twelve 44px interactive cells, no sticky Objective/Hint or movement | Use already-permitted compact noninteractive statuses/grouped detail; normal-text simultaneous visibility and accessible extreme-text scrolling |
| Initial title playback differs from Sound | `App.tsx` 754, `musicTransport.ts` 105, `music.ts` 137; same-context early return never applies initial title URL | Actual player URL and snapshot agree from first gesture |
| Accepted maze shuffle is lost | `App.tsx` 1458 and removed picker calls; adapter chooses maze pool index 0 on every story return | Preserve current no-repeat maze-selection behavior behind the port |
| Responsive logo fallback can fail | `CatalogueImage.tsx` 42–45 retains srcSet and blacklists the wrong URL when currentSrc differs | True failed-resource handling and bounded approved fallback at both responsive sizes |
| Long dialog keyboard reading is unproven | `DialogShell.tsx` 53/65; scroller lacks explicit tab/focus contract and trap ignores implicit scroll focus | Keyboard reaches/scrolls all text and returns safely without programmatic-scroll assistance |
| Required presentation art absent | Equipment remains 64px optical fallback; 14 approved-source identities inventoried in UI_UX_SPEC | Deterministic approved-source presentation derivatives, actual visible-subject/alpha/DPR/network proof and separate bytes |
| Tests miss mandatory geometry | `ui-overhaul.pw.ts` 145–147 checks horizontal cells/generic 44px, permits phone scrolling | Vertical/simultaneous containment and 48px frequent/44px other target assertions |
| Teaching search lacks response-cost evidence | `powerGuidance.ts` 11 in App render memo at 861, up to 2,048 engine states | Demanding real-encounter latency; prompt exact feedback and bounded optional suggestions |

Line numbers identify this candidate; follow symbols after edits. The image
and audio findings are source-confirmed review findings, with regression tests
required in the correction. The dialog row is an acceptance gap requiring
explicit browser/WebView behavior, not a claimed physical screen-reader result.

## Root allocation decision

Approve the existing bounded request: **4,500 gzip-9 JS / 0 CSS / 43,795 public bytes**.
Root independently verified ledger arithmetic, final hashes and source provenance.
The resulting ceilings are 139,018 JS, 30,227 CSS and 152,925,510 public bytes.
Candidate usage is 138,938 / 15,742 / 152,925,510 respectively. Source-matched JS+CSS
saves 9,976 compressed bytes; the 39,408-byte WOFF2 plus 4,387-byte OFL adds 43,795,
so mixed code/public delivery increases 33,819 bytes. This is measured value,
not a timing qualification or permission to call the entire UI accepted.

The follow-up applies this approval to the still-requested runtime ledger and
remeasures after corrections. Extra art/code requires its own exact return.
Do not silently absorb it or treat the current 80-byte total JS slack as a new
feature budget. Existing source/rollback/licence records remain required.

## Bounded art responsibility

Static art approvals stay closed. Root delegates deterministic derivative
preparation from the 14 existing approved equipment masters to the same task
under `../plans/AGENT01-review-follow-up.md`; this is a narrow dependency repair,
not a reopened art phase or authorization for new generation. The established
publisher/catalogue/provenance pipeline remains authoritative. Root reviews
actual-size/alpha/registration and incremental allocation before acceptance;
an unreproducible source is returned by exact ID. Additional earned-detail DPR
gaps remain explicit and cannot be hidden by optical fallback.

## Verification and next retry

Root's default `npm run check` returned 435 passes and 9 timeout-only failures
across the 3 existing generator/level/portal solver test files; build did not run.
Duration 154.58s. This matches a previously observed host-contention risk but is
not a passing command or proof of cause. `npm test -- --maxWorkers=1` then
returned 441 passes and 3 timeouts in 195.52s: generator size variety, Moonlit
Friendship Quest underpowered-combat witness, and complete campaign solver
validation. No assertion-result mismatch was reported, but both commands failed.
The engine/generator/level source and tests are unchanged by this UI candidate;
cause remains unqualified. No test threshold or seed was changed. Recheck these
gates after the correction on a suitable host; do not describe serial as green.

The UI candidate remains uncommitted. Only independently reviewed programme/
review documentation is eligible for the current GitHub checkpoint. The Human
copies the bounded follow-up into the existing Agent 01 task. After root accepts
its returned evidence: commit/push UI → implement/review MOVE-01 → clean-source
FP-UI1 web/Windows release verification → family checklist/feedback → Agent 04.

Future PT32 adjustable camera view is in Plan08, outside FP-UI1. Playtest feedback
uses `../playtests/FP-UI1-feedback-template.md`; no download is ready from this
candidate, and an older package must not be presented as this build.
