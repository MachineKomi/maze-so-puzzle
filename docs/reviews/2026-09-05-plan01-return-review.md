# Plan 01 — root review of the six-correction return

Date: 2026-09-05. Candidate base: `09413c175c5776044e4ab5dd6eddf31804d33d34`.
Decision: **accept the reviewed UI engineering checkpoint for MOVE-01 and the
planned FP-UI1 preview**. No playable release, physical-device qualification or
Human comfort acceptance is claimed by this source checkpoint.

## Review disposition

Root read the complete Plan 01/addendum, correction prompt and returned evidence,
inspected the changed implementation and obtained independent code/art reviews.
The six returned corrections close the identified compact-phone, music,
responsive-error, keyboard-reading, equipment-presentation and Power-search gaps.
Root independently reproduced all 452 project tests and the matching production
build; the follow-up browser evidence records 26 passing checks. These are
engineering checks, not physical-device or Human comfort acceptance.

Root completed the remaining fifteen earned-reward DPR2 renditions using the
existing approved Batch 22 originals and deterministic pipeline. Their 256px
delivery files were first reproduced exactly; no identity, original, field sprite
or optical registration changed. Independent review inspected all fifteen new
images at original resolution and root inspected the actual-size alpha sheets.

The fourteen equipment renditions and fifteen rewards have separate, hash-bound
root publication decisions in `docs/source-assets/publication/`. Candidate
reports remain immutable historical preparation snapshots; their candidate
wording does not override the later `*-approval.json` decisions and published
source records. The publisher checks original, output, report and candidate-record
hashes and only promotes the reviewed metadata. Six regression tests cover
tampered identity/provenance, duplicate membership and insufficient file limits.

Equipment retains the inherited 102,400-byte ceiling except four explicitly
reviewed files: bubble-ring-blade 123,658; splash-boots 162,478; spring-boots
182,130; antidote-leaf 118,676. Each reward gets its exact measured file ceiling.
These are bounded delivery exceptions for approved-source sharp presentation,
not a general increase in art budgets or reopened Human source-art approvals.

Git attributes preserve the exact bytes of these new hash-bound publication
records, their frozen prompt/map inputs and delivered font/licence. The existing
runtime-map input has explicit CRLF checkout encoding, matching its already-
measured bytes; no stored JSON value or runtime pointer changed.
This prevents a fresh Windows checkout from invalidating recorded hashes or
changing the exact public-byte allocation. Source rebuilds use the recorded
Windows encoder environment; a different encoder/platform needs requalification.

## Root correction and responsive evidence

Adding reward renditions exposed a first-render inefficiency on short DPR2
phones: the provisional 200px size fetched 512px before layout measured 128px.
Root made the initial choice match the existing compact-height CSS breakpoint;
ResizeObserver remains the authority after layout/resize. No larger image is
preloaded merely because it is in the catalogue.

The shared browser check now covers all 29 identities at 960x720 and 568x320,
DPR1/2 (116 selections), actual visible-subject size, sufficient rendition,
delayed decode and approved fallback. Rewards use 256px at 128px/DPR2 or
200px/DPR1, and 512px at 200px/DPR2. The first failing network run is retained;
the corrected run passed all three targeted browser checks, including real
completion, exactly-once reward credit and earned-Book inspection.

## Exact delivery allocation decisions

Root approves the returned `ui-plan01-review-correction-and-presentation`
allocation: **2,522 gzip9 JS / 0 CSS / 1,367,574 public bytes**.
Root separately approves `ui-plan01-earned-reward-dpr2-closeout`:
**667 gzip9 JS / 0 CSS / 2,686,552 public bytes**. Neither includes reserve.
The reward decoded upper-bound sum is 15,728,640 bytes; the equipment sum is
14,680,064. These are per-file bounds, not measured simultaneous residency.

Final measured delivery is **142,207 gzip9 JS / 16,278 gzip9 CSS /
156,979,636 public bytes**. Approved ceilings become **142,207 / 30,227 /
156,979,636**. The retained Vite advisory is not a failed compilation. Relative
to the original runtime baseline, combined compressed code still saves 6,171
bytes; public growth is 4,097,921 bytes including the local font/licence.

Measured JS `index-D4Afxyo2.js` SHA-256:
`d6cbd63e38267ce35c507c8658709d40a56c41679338604e4fedbb32c6cf3a5f`.
CSS `index-BodusrMY.css` SHA-256:
`920b43dede508ecd15c7f26834fb9edc1e1ddee52fae1e6738fb9799f53333bc`.
The build marker and source-matched inventory identify this candidate; subsequent
MOVE/release changes need their own measurements and allocation if necessary.

## Evidence and remaining boundary

Root logs: `C:/Users/hellb/AppData/Local/Temp/maze-ui01-astra-review-20260905/`.
Root corrected browser evidence and alpha sheets:
`C:/Users/hellb/AppData/Local/Temp/maze-ui01-astra-media-20260905-r2/`.
Original specialist evidence remains linked in `../UI_UX_REVIEW_EVIDENCE.md`.
Targeted root UI art tests: 6 passed. Approval regression tests: 6 passed.
Locked Cargo compilation passed; this is not a packaged Windows launch.

Final root verification passed: fourteen equipment and fifteen reward outputs
reproduced byte-exactly with reviewed publication metadata; `art:proof` passed;
`art:check` has zero errors and 420 retained historical/pending warnings;
`art:test` passed all 121 tests; `npm run check` passed all 453 project tests and
TypeScript/Vite; `perf:check` passed eleven scenario contracts, all byte limits
and three evidence schemas. `git diff --check` passed. No seed, timeout or budget
checker was weakened. Required exact allocations are recorded above.

The six-correction candidate's 26-check browser report plus root's three focused
29-image checks support this engineering decision. Plan01 is not claimed fully
device-qualified: WebView2 interaction and actual portable launch are required
in the upcoming FP-UI1 packaging step. Human visual,
comprehension, physical device, screen reader, controller, couch readability and
comfort reviews remain explicit FP-UI1/downstream checks. Dirty-host timing is
report-only. The root travel improvement follows UI acceptance; it is not
retroactively part of Agent 01's work.

All 315 raw specialist/root media evidence files were copied and hash-verified
under `C:/Users/hellb/Documents/Maze so Puzzle/review-evidence/2026-09-05-plan01/`.
The tracked `2026-09-05-plan01-evidence.json` indexes those copies. Raw screenshots
are outside runtime/Git; this local evidence copy is not Plan12's Human external-
backup confirmation and authorizes no asset deletion.

Rollback: restore the UI runtime to `6493110795d83bd799481e0efa5a5fd228360098`
only through a reviewed scoped change, preserving later root planning, save
semantics and all approved originals. For the media closeout alone remove the
new rendition binding and return to approved 256px/optical fallbacks; retain all
versioned files until Plan 12's copy-first retirement gate. Do not reintroduce
the six reviewed defects merely to repay bytes.
