# v0.22.9 WALL-04A — engineering acceptance

Root Astra implemented and actual GPT-5.6 Sol independently reviewed.
Frozen runtime `cbe8ab879bd58c0a08a770209ebfeb649f684e2b`.
Source/browser/final-native accepted; exact-source CI34034034559 and Production
6293082169 succeeded. Canonical HTML/JS/CSS bytes match the tested build.
The immutable [manifest](../../release/WALL-04A-v0.22.9-manifest.json) owns exact
commands, evidence hashes, scope and retained limitations. Publication/download
closure is recorded later in its release-verification receipt, not inferred here.

## Visible result and limits

Rounded inward wall faces, signed material-local bevels and floor-only contact
replace the old blurred silhouette. Smaller world-anchored floor/wall/dressing
repeats and a pale moon-slate floor clarify scale and Springstep's floor/wall read.
PT33's reproduced edge came from a theme ornament sharing an animated30%-width
inset-shadow pseudo with the viewport frame. Separate ownership and full fixed
frame bounds remove that reproduction; physical confirmation is still P13.

No new art/media/dependencies, gameplay/content/save changes, camera/FOV change,
actor/held lighting or hole replacement. Full/Lite/Static retain structural depth.
New depth is filter-free; existing hazard filters remain. Memoized terrain does
not mutate on ordinary camera movement. No universal frame-rate/GPU/iPad claim.

## Evidence

- Final project631/631 across56 files, TypeScript/Vite, locked native release,
  production audit0 vulnerabilities, deterministic performance contracts pass.
- Fresh art142/142; current validation0 errors/429 disclosed historical warnings.
- All12 material pairs and4 cardinal directions reviewed in actual-component
  rack; all512 small wall occupancy patterns, normals, arc signs, inward masks,
  disjoint bands and unique SSR SVG identities tested. Actual Sky/Twilight/
  Rainbow scenes inspected. Old/new rack uses new calibration on both sides.
- Frozen browser R3:36/36 moving combinations (DPR1/2, Full/Lite/Static,
  Full/Reduced,3 legal engine/solver fixtures), exact frozenJS, zero terrain
  mutations, invalid frame samples, broken images or errors. Loaded-host timing
  has long tails and remains report-only, not performance qualification.
- Final native portable173471744B, SHA256
  `4bf84655dc589447344397c26847351990156743529a623d4aaafedfad46a441`.
  Actual controls: Twilight27@x6 -> keyboard Left28@x5 -> pad Right29@x6,
  Lite->Full, normal close/reopen/Play/Continue restores exactly all raw saved
  progress/run/preferences.29steps/two friends/Full,696px frame,lighting04a-v1,
  broken0. Isolated owned QA profile; no Human state touched. Sol independently
  audited final process closure and inspected final screenshots.
- JSgzip9 158643 (+1474), CSS24041 (+61), public165031011 and decoded-image
  inventory411582176 unchanged. Allocation1500/61/0 passes. Portable+2048B.

## Rejected attempts and rebuild provenance

The first square-corner candidate was revised; the first rack had duplicate
SSR IDs and is invalid evidence. The original camera probe observed transform,
not individual translate, so it did not qualify moving-camera coverage. Two
later runs missed an observation; R3 adds a first-sample barrier and before/after
translate checks. No production assertion or timeout was relaxed. One early
concurrent project run hit the unchanged generator30s timeout; serial full passed.

Locked npm ci initially hit an owned preview's Rolldown file lock; after stopping
only that owned process, clean install passed. Main checkout changed raw source
bytes while Git source and built dist stayed identical. The old raw-input marker
failed, correctly. A real locked rebuild created current input marker
`95bf63f9372dd88f74100103c7fd480c036af1fafe3dc337bd98df5a456d422e` and identical
dist`3385e3c7b8a9bf070b54f265d718bfa2ca07183504ca48a3ed61dc4abfd7cc2e`.
The PE differed, so it was restaged and actually retested; old native observations
were not promoted. First-build files remain recoverable externally. No blanket
fingerprint/EOL waiver or marker-only rewrite. Cargo's EOL/index notice had equal
Git content hashes and no source diff; refreshing its index staged no change.

## Follow-on

Plan02 reward-first presentation is next: wall-bouncing committed Gold/Science/
maze-Power, magnetic collection, bounded trails and sound. Persistent XP remains
Plan14, not mislabeled Power. HOLE-02,04-B/04-C/PT36, actor first-paint follow-up,
P5–P13, physical iPad/clean-host/signing gates remain open. This is not whole
Plan04 completion. Roll back the bounded presentation through a new commit;
retain v0.22.8 and all player data.

[Independent Sol record](2026-09-06-wall04a-sol-final-review.md) ·
[Player checks](../../release/WALL-04A-v0.22.9-PLAYTEST.md).
