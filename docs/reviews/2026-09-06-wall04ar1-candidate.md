# WALL-04A-R1 candidate evidence

Root Astra,2026-09-06. Runtime baseline `feb04e1` (web0.22.10); requirements
checkpoint `d9c7697` is documentation-only. [Brief](../plans/WALL-04A-R1-convincing-depth.md).

**Paused by Human for laptop migration; not promoted or deployed.** See
[handoff](../migrations/2026-09-06-new-laptop-handoff.md) and
[raw numeric evidence](../migrations/wall04ar1-paused-evidence.json).

## Source and visual judgment

Both original and upshifted complete bevel bands contribute to the exact
intersection top mask. Same-response overlap is filled once; rear rims are not
lost. Colored opaque front base plus compressed existing texture makes a face,
not a translucent stripe continuing the top texture. Profile height0.20–0.26,
bevel0.045–0.055. The fixed downward side is independent of source bearing.

Cast is a translated evenodd cap and finite outward straight/rounded ribbons in
one opacity group, clipped to ordinary floor. Maximum reach0.221tile in current
profiles (absolute cap0.24); independent contact reaches0.0375tile. No wall/hazard/
pit receiver or new collision/occlusion. Cardinal light only, same legacy bearings.
Four additional wall paint paths, zero new wall filters, zero media/CSS/dependencies.

Root has inspected all six material families with top/bottom/right/left light,
32px grayscale and48px color in the checked-in dev-only rack
`scripts/performance/wall-relief-rack.html`. The faces are materially deeper and
rounding is coherent; this remains stylized relief rather than perspective3D.
Dark/foliage detail stays subordinate and paths remain readable. Human's higher
beauty goal remains open; if still disappointing, compare real orthographic3D
under the brief instead of declaring this finished. No physical-iPad claim.

Screenshots remain local ignored proof evidence until the final receipt; the
rack source reconstructs them without committing another full image packet.
Early rack missing-favicon404 was corrected with a data favicon; it was not a
runtime missing asset. Early stale browser refs were resnapshotted, not bypassed.

## Qualification status

- 28 focused geometry/render tests pass, including all512 small occupancies ×4
  bearings, original/projected bands, finite tip closure, masks and bounded nodes.
- 642 project tests pass serially across58files,114.98s on this loaded laptop.
- TypeScript and production build pass. Candidate before version bump measures
  JSgzip9=161595 versus161288 baseline (+307); CSS23655/public155542751 unchanged.
  Root allocation ceiling400 remains below the2000 prototype cap.
- Existing artwork and retirement inputs unchanged: previous152art tests /
  zero validator errors are inherited, not a fresh art-suite claim.
- Actual Sol accepted source, rack and the three final production gameplay
  captures as a provisional stronger-relief candidate; no visual/source blocker.
  This is not Human beauty acceptance or physical/native performance acceptance.
- All72 final-version production camera cases pass:3 legally derived save scenes,
  1193×833 and844×390, DPR1/2, Full/Lite/Static, Full/Reduced motion. Every case
  moved the camera, with0 terrain-SVG mutations/errors/broken images/invalid
  ghost-frame samples. This is functional/mutation evidence, not a60FPS claim.
- Final build and `cargo check --locked` pass. Measured JSgzip9=161595 (+307),
  CSS23655/public155542751 unchanged; approved400JS allocation passes.
- Alternating warmed baseline/candidate Twilight/DPR2 trace: baseline Full p95
  ~16.9ms in3 repeats, candidate16.9/33.3/33.2ms. TotalPaint and RasterTask cost
  were similar, but the p95 signal must not be dismissed as “old laptop” proof.
  Later same-candidate cast-on/off Full was~16.8–17ms both and Lite~33.2–33.5ms
  both. This weakens a cast-specific hypothesis; it does not clear the original
  signal. CompositeLayers timing was unavailable, not zero GPU cost.
- New-host controlled review, release identity and deployment remain pending.
  Human explicitly paused further testing/development; no new runs after pause.
- No native binary is published by this candidate; previous0.22.10 native
  qualification remains open. Published Windows0.22.9 remains available.

## Rollback

Restore the complete R1 renderer/profile/version seam to `feb04e1`, preserving
new requirement documents, prior reward renderer, saves and asset-cleanup receipt.
No asset or profile migration required. The `wallLighting=legacy` diagnostic
remains available but is older pre04-A geometry, not an exact04-A comparison.
