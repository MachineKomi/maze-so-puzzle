# Book completion and moving-dot repair

Astra, September7. Candidate on `codex/book-completion-hazard-refinement`, based
on aa46caa. Live remains web0.22.15. This is a backed-up WIP, not publication.

Actual Book-launch browser proof reproduced the old partial-rescue Stay focus
and tester same-ID active-card classification. Next already computed the next
story index correctly; its transaction is unchanged. Next/Surprise is now always
primary, with explicit Stay and Restart retained. Book resumes only normal runs;
same-ID tester selection begins a normal story. Temporary newer-profile sessions
have truthful wording, and supported save denial leaves the exit pending.

Ten Book cases pass: middle partial/perfect Enter/click (including held/double
activation), explicit Stay/re-entry, tester-to-normal same-ID selection, final
partial/perfect restored approaches, denied save/retry and schema999 byte
preservation. Final approaches use engine-derived valid late snapshots after
real Book launch, not a claimed complete browser replay. Actual normal entry
still legitimately discovers the visible caged unicorn; tester mode does not
award or discover it. Earlier full-JSON equality incorrectly rejected that
normal discovery and was corrected without changing runtime discovery.

The Human's yellow dot matches the old amber `player-layer::after` sparkle:
baseline capture shows opacity0 at rest/.637725 during travel, yellow text and
4px amber shadow. Independent programme review found the same CSS in0.22.10
and older source. Removed its movePulse state/classes and keyframes; the ground
ellipse no longer shares its pulsing animation. Actor travel/sway is retained.
Full/Lite/Reduced/Static all pass stationary/moving/rested pseudoelement checks:
no corner sparkle, steady opaque ground owner, actual nontransparent paint.
Lite uses its existing gradient, so its test accepts gradient color stops rather
than requiring the solid fill used by the other modes.

Independent actual Sol inspected source and baseline/candidate pixels. Astra
implemented Sol's temporary-copy, real partial/perfect cohort and ground-color
assertion corrections. Final source/release review remains pending.

Evidence under `C:/GameDev/maze-game-qa/performance/`: book03-baseline (two
expected failures), move02-baseline (one expected failure), book03-candidate
(10/11 before test correction), book03-reviewed (10 Book +3 MOVE pass, one Lite
solid-fill test error), and move02-modes (all four pass), all suffixed20260907.
Retain all failed diagnostic packets. Existing dist reused; no media/dependencies
added. Pre-release gzip9 JS165915/CSS24026, public155542751B, within current caps.
Build passes; final combined source, project suite, browser/performance and
public qualification still required. Next implement [HAZARD-03](../plans/HAZARD-03-connected-living-surfaces.md).

**Publication update:** the final qualified behavior is delivered in
[web0.22.16](2026-09-07-v02216-public-verification.md). This candidate record retains
its original source/test identities; the final qualification and public receipt
own release acceptance and its limits.
