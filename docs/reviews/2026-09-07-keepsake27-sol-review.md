# DELIGHT-27 independent Sol review

**Reviewer:** Sol (GPT-5.6), independent read-only review  
**Candidate:** `fdc60f2` on `codex/earned-keepsakes`  
**Status:** supports bounded web promotion

## Independent findings

The earned-event adapter is attached to the correct durable boundary. It derives sticker, medal and badge differences from the actual before/after profiles only after `writePlayerProgress(nextProgress)` succeeds. The next level is loaded before the event is admitted, so ordinary level cleanup cannot erase the new event. Tester completion, Stay, denied active-run or profile writes, unsupported future-profile continuation, currency-only changes and already-owned awards do not create an event. A failed active-run clear after a successful profile write does not revoke the earned award. The earlier victory-time reward timer was removed, closing its pre-commit and after-Stay sound leak.

The presentation is one non-modal, pointer-transparent card inside the existing board. It waits silently behind the next chapter story and other critical presentation, plays one existing reward or stamp cue only when it becomes eligible, and is bounded by a 4.5-second deadline. First movement, navigation, an active overlay, visibility loss, portrait transition or resize discards it; none of those paths pauses it for replay. Full and Lite use finite 900ms and 450ms image motion, while Reduced and Static stay still. The deadline-based effect restores its remaining timer after React StrictMode's development effect replay without replaying the sound.

My first review found that the original 36% by 40% corner card had no legal placement for Ame at local `(2,2)` in a 6 by 6 camera. Six current curated Classic starts can use that geometry, and the event was silently suppressed. A first width-only fallback then produced an 84.45 by 120.64px card whose text overflowed and whose art was only 37.65px high. The final geometry keeps the full 3 by 3 protected region and selects a 60% by 28% horizontal fallback. Pure checks cover every current curated Classic start and every whole-tile position in a 6 by 6 view; current Surprise mazes are at least 9 by 9.

I freshly inspected the final mounted packet in `C:/GameDev/maze-game-qa/performance/keepsake27-final-browser/earned-keepsakes`. The repaired centered Classic proof is 180.96 by 84.45px with 60.10px art, complete copy and no scroll overflow. The 780/844 phone folded, expanded and Classic compositions, 1280 compositions, enlarged text, generated destination, Lite, Reduced and Static samples are readable, remain inside the board, avoid Ame's protected tiles and do not overlap the separate HUD or pad. I also inspected the later medal and badge milestones in `keepsake27-extra-r2`: both use the correct approved large art and copy, and the badge is featured when the saved difference contains one. This is a fresh visual review of those images, not Human beauty approval or physical-device acceptance.

## Final evidence and disposition

The final source-matched browser evidence covers 39 distinct affected contracts across a clean 33-case run and two corrected follow-ups contributing three new contracts each; it is not one 39-case clean run. It covers successful-write/failed-clear, denied writes, tester and Stay, story waiting, generated no-story activation, active-overlay and hidden cancellation, first-move dismissal, StrictMode and real-time expiry, no recovered-receipt replay, all award families, centered and enlarged geometry, and comfort modes. The audio checks observe no pre-commit cue, exactly one stamp at SFX gain 0.85 after activation, and zero output at gain zero. The initial geometry failures, the mistaken future-profile Continue fixture and the first milestone expectation are retained as corrected evidence rather than hidden.

The three paired reports contain 72 rows, including 60 measured rows and four warmups per cohort. Their SHA-256 values are `e9b4e475fcf9c8fb0d8e54938dd34e94cbab6f0916198ea9bd45a6e1e638712d` for ordinary movement frames, `cb5d1d13487ff8fc8828faf562487541059962b9b8eeaa51e2c35d9f742e8936` for earned frames and `8f5b0edf01ef6fa8fd4248c84b3457f2637ea37824df24806177d0228d7564a8` for earned traced work. Every row binds candidate source `fdc60f279c2c08e0aed8992ecc6dea55a0b391c7` and JS SHA `7e15cacdefbfe03e0b57d7f6421a6641614824890dd1f50393044539ee6f722c`; all measured routes have zero errors, broken images and terrain mutations.

Median p95 stays about 16.8ms in every frame group. The ordinary 844 movement control has a small unfavorable change from 3 to 5 intervals over 20ms across five pairs, with worst 33.4 to 33.5ms and no interval over 34ms; the 1280 control improves from 3 to 1. On the earned route, 844 untraced intervals over 20ms improve from 1 to 0 and 1280 from 2 to 1. Traced phone work retains one 50.1ms interval on each side; desktop retains five intervals over 20ms on each side. The card adds about 5.62ms and 5.80ms of Layout over the measured phone and desktop route, while Paint changes by +0.51ms and +4.40ms and RasterTask by +1.14ms and +0.61ms. These are bounded route totals, not per-frame costs.

At its peak the candidate mounts exactly one card, six DOM nodes and one image. After expiry, nodes, images and final layer counts return to their matching baseline values. The candidate uses no new public media and remains within the recorded budgets: JS gzip9 is 180,835 bytes against 181,169, CSS gzip9 is 26,848 against 31,622. The frozen public web26 baseline was separately byte-matched before comparison.

I support promotion of `fdc60f2` for this bounded web presentation. The finite saved-keepsake moment is proportionate to its measured work, its durable transaction semantics are correct, and the unfavorable 844 movement count is small, isolated to the ordinary control and below 34ms. It should remain disclosed rather than described as strict parity.

This review does not close physical Apple, native, memory/GPU, Human satisfaction or broader DELIGHT-02B work. The Book remains the durable authority even when the decoration is skipped. DELIGHT-02B A and B were delivered separately; this candidate concerns only the earned-keepsake C presentation.
